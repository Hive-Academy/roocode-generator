/* eslint-disable @typescript-eslint/no-unused-vars */
import { TemplateManager } from "./template-manager";

// describe("TemplateManager Integration Tests", () => {
/*
  // let manager: TemplateManager;

  // beforeEach(() => {
  //   manager = new TemplateManager(undefined, { templateDir: "templates", templateExt: ".tpl" });
  // });

  // test("should resolve template path using configured templateDir and templateExt", () => {
  //   // Access protected method via subclass for testing
  //   class TestTemplateManager extends TemplateManager {
  //     public exposeGetTemplatePath(name: string): string {
  //       return this.getTemplatePath(name);
  //     }
  //   }
  //   const testManager = new TestTemplateManager(undefined, {
  //     templateDir: "templates",
  //     templateExt: ".tpl",
  //   });
  //   expect(testManager.exposeGetTemplatePath("myTemplate")).toBe("templates/myTemplate.tpl");
  // });

  // test("should not have hardcoded template paths elsewhere", () => {
  //   // This test is a placeholder to ensure no regressions on hardcoded paths
  //   // In a real scenario, this would be part of a larger integration test suite
  //   expect(true).toBe(true);
  // });
*/
// });
